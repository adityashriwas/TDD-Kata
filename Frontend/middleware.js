// Optional middleware placeholder. Avoids relying on backend HttpOnly cookie on a different domain.
// You can implement redirects based on your own cookie/session if needed.
export default function middleware() { /* no-op */ }

export const config = {
  matcher: [],
};
